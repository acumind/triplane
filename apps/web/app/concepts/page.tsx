import { ConceptIndex } from "../../components/ConceptIndex";

/** The index always answers here, whether or not "/" is the product pitch. */
export const dynamic = "force-dynamic";
export default function Concepts() {
  return <ConceptIndex />;
}
